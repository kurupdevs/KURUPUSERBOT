services:
    kurupuserbot:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: kurupuserbot
        restart: unless-stopped
        env_file:
            - .env
        networks:
            - default

networks:
    default:
        external: false